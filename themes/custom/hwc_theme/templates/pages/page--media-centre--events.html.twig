{% extends "page.html.twig" %}getFileUrl() must be of the type string or null, object given, called in /var/www/html/vendor/twig/twig/src/Environment.php(418) : eval()'d code on line 107 in Drupal\Core\Template\TwigExtension->getFileUrl() (line 284 of core/lib/Drupal/Core/Template/TwigExtension.php).

    {% block banner %}
      {% if not is_front %}
        {% if page.breadcrumb %}
          <section class="section-title">
            {{ page.breadcrumb.hwc_theme_breadcrumbs }}
            <h1 class="page-header">{{ page.breadcrumb.hwc_theme_page_title }}</h1>
          </section>
        {% endif %}
      {% endif %}
    {% endblock %}

{% block content %}

  <section class="events-list-page gray-background">
    <div class="container">
      <div class="row">
        <aside class="section col-12 col-lg-3 sidebar-first" role="complementary">
          {{ page.sidebar_left.exposedformevents_searchpage_1 }}
          {{ page.sidebar_left.newsletter }}
        </aside>
        <section class="section section-center col-12 col-lg-9 events-list">
          {{ page.sidebar_left.exposedformevents_searchpage_1 }}
          {{ page.content.hwc_theme_content }}
        </section>
      </div>
    </div>
  </section>

{% endblock %}
