display:
  default:
    display_title: Default
    display_options:
      exposed_form:
        options:
          reset_button_label: Reset
      pager:
        options:
          expose:
            items_per_page_label: 'Items per page'
      fields:
        filename:
          label: Name
        status:
          label: Status
      filters:
        status:
          expose:
            label: Status
  page_2:
    display_options:
      pager:
        options:
          expose:
            items_per_page_label: 'Items per page'
      fields:
        type:
          label: 'Entity type'
      arguments:
        fid:
          exception:
            title: All
