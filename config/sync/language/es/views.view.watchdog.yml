display:
  default:
    display_title: Default
    display_options:
      exposed_form:
        options:
          reset_button_label: Reset
      pager:
        options:
          expose:
            items_per_page_label: 'Items per page'
      fields:
        severity:
          label: Severity
        type:
          label: Type
        message:
          label: Message
        name:
          label: User
        link:
          label: Operations
      filters:
        type:
          expose:
            label: Type
        severity:
          expose:
            label: Severity
